# Meeting Recorder

Запись звонков на macOS одной кнопкой: приложение пишет твой микрофон и
системный звук — голоса собеседников в Zoom, Meet, Telegram, браузере.

Виртуальные аудиодрайверы (BlackHole, Loopback) не нужны: системный звук
берётся через ScreenCaptureKit.

## Требования

- macOS 13 или новее
- Command Line Tools: `xcode-select --install`

Xcode-проект не нужен, сборка идёт напрямую через `swiftc`.

## Сборка

```sh
./build.sh
open MeetingRecorder.app
```

Скрипт собирает бандл `MeetingRecorder.app` и подписывает его ad-hoc — без
подписи macOS забывает выданные разрешения после каждой сборки. Приложение можно
перетащить в `/Applications`.

## Разрешения

Выдаются один раз при первом запуске:

| Разрешение | Зачем | Где включить |
| --- | --- | --- |
| Микрофон | твой голос | запрос при старте записи |
| Запись экрана и системного звука | голоса собеседников | Системные настройки → Конфиденциальность и безопасность → Запись экрана |

После включения записи экрана приложение нужно перезапустить.

Проверка: если во время записи индикатор «Zoom / системный звук» не двигается,
второе разрешение не выдано.

## Результат

`~/Documents/MeetingRecorder/2026-08-17_14-30-12/`

| Файл | Содержимое |
| --- | --- |
| `mix.m4a` | обе дорожки в одном файле |
| `mic.caf` | только микрофон |
| `system.caf` | только собеседники |

Отдельные дорожки остаются для расшифровки по ролям и для перемикширования,
если один голос тише другого.

## Как это работает

- Микрофон — `AVAudioEngine`, тап на входном узле.
- Системный звук — `SCStream` из ScreenCaptureKit, звук самого приложения
  исключён, поэтому эха нет. Наушники не мешают: сигнал берётся до вывода на
  устройство.
- Обе дорожки приводятся к 48 кГц float32 и пишутся в `.caf` независимо.
- На стопе дорожки складываются в `mix.m4a` с выравниванием по времени первого
  сэмпла, так что задержка старта одного из источников не даёт рассинхрона.

## Структура

```
Sources/
  MeetingRecorderApp.swift    точка входа
  ContentView.swift           интерфейс: кнопка, таймер, индикаторы уровня
  RecorderController.swift    состояние записи, папки, сведение
  MicCapture.swift            микрофон
  SystemAudioCapture.swift    системный звук
  Mixdown.swift               сведение дорожек
  AudioSupport.swift          формат, запись в файл, конвертация
build.sh                      сборка бандла
Info.plist                    идентификатор, версия, описания разрешений
```

## Лицензия

MIT

Записывать разговор стоит, предупредив участников: в ряде стран согласие
обязательно по закону.
